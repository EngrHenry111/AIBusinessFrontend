# Loaded automatically by `vite build`. Acts as the default backend URL when the
# host (Vercel) does not inject VITE_API_URL. A real Vercel env var still wins.
VITE_API_URL=https://businessai-backend-6g8l.onrender.com
